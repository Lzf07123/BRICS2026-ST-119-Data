# 知识库使用契约

> 版本：v1.0（2026-09-03）  
> 定位：本文件只约定「中国国内旅行管家」知识库的数据范围、检索方式、引用边界、分段配置和验收口径；不定义 Agent 人设、开场白或完整提示词。  
> 数据负责人：林知枫；使用方：Agent 搭建与测试协作者。

## 1. 权威数据源

| 库 | 文件 | 条目前缀 | 首要用途 |
| --- | --- | --- | --- |
| 目的地库 | `01-目的地库.md` | `D` | 目的地选择、季节、天数、预算、交通枢纽 |
| 景点库 | `02-景点库-<目的地>.md` | `A` | 景点推荐、门票预约、开放时间、交通与避峰 |
| 美食库 | `03-美食库.md` | `F` | 城市美食、招牌、人均、营业时间、场景推荐 |
| 行程模板库 | `04-行程模板库.md` | `R` | 多日行程骨架、逐时段安排、预算与备选方案 |
| FAQ库 | `05-FAQ库.md` | `Q` | 抢票、避峰、雨天、特殊人群等高频兜底 |
| 交通住宿库 | `06-交通住宿库.md` | `T` | 机场/高铁接驳、线路、住宿区域和价位 |
| 文化讲解库 | `07-文化讲解库.md` | `C` | 文化背景、导览词、关联地点 |

`00-数据字典.md` 是字段 schema 和生产规范，不建议作为旅行内容的检索来源。`联网核查报告`、`自检报告`、`重点抽查清单`、`上传与检索验证清单` 只用于本地复核，不得作为答案语料或对外引用来源。

## 2. 条目边界

1. 每条记录是一个独立 Markdown 块，块间用空行分隔。
2. 每块首行必须是 `### 【条目ID】名称｜城市`；景点库首行额外附星级。
3. 景点、美食、交通、文化条目应保持单条自包含；禁止依赖“见某条目”式引用。
4. `关键词` 是检索主入口之一，收录别名、俗称和常见问法；目的地、景点、美食、文化讲解四表必填。
5. 时效字段包括门票与预约、开放时间、营业时间、班次频率、人均、费用区间。库内标注“（待核实）”时，回答必须同步提醒用户以官方渠道为准。
6. 知识库没有的字段不得由使用方脑补。缺店名、地址、价格、班次、评价、统计数字时，只能说明未收录或建议官方核实。

## 3. 检索路由

| 用户意图 | 首先检索 | 辅助检索 | 重点读取字段 |
| --- | --- | --- | --- |
| 选目的地、比目的地 | 目的地库 | 行程模板库 | 定位标签、建议天数、预算档、最佳季节 |
| 景点推荐、门票预约 | 景点库 | FAQ库、交通住宿库 | 星级、门票与预约、开放时间、交通指引 |
| 吃什么、找店 | 美食库 | 景点库 | 城市、人均、招牌必点、营业时间、适合场景 |
| 多日行程规划 | 行程模板库 | 景点库、交通住宿库 | 天数、逐时段安排、预算估算、备选方案 |
| 突发、闭馆、抢票 | FAQ库 | 景点库 | 问题、答案、关联主题 |
| 机场、高铁、住宿 | 交通住宿库 | 目的地库 | 线路、时长、费用、区域、价位档 |
| 导览、文化背景 | 文化讲解库 | 景点库 | 关键词、导览词、关联地点 |

匹配顺序：城市/目的地 → 关键词/别名 → 条目名称 → 星级/人群/预算/季节。禁止跨城市错配字段；多目的地组合时必须先确认地理路线合理。

## 4. 组合与回答边界

1. 单点问题优先使用单条记录，不混入无关库的推荐。
2. 行程类回答以行程模板库为骨架，再按同城市景点、美食、交通记录补足执行细节。
3. 优先匹配适合人群、预算档、季节和体力节奏；五星/四星是强推荐，不是唯一答案。
4. 保留库内硬信息，不得把“待核实”改写成确定结论。
5. 知识库未命中时，回答“知识库暂未覆盖”，不要退回不可靠常识；如平台已配置搜索工具，也只能作为明确标注的动态核验，不伪造知识库来源。
6. 不使用知识库外的店名、地址、价格、班次、评分、评价或统计数字补写方案。
7. 不输出密码、证件号、手机号等敏感信息，也不替代医疗、法律、公安和紧急救援机构判断。

## 5. 分段与上传

首选语义分段：

- 分隔符：三级标题 `### `；
- 记录间分隔：空行；
- `overlap`：0；
- 不允许在字段块中间截断；
- 一个目的地、景点、美食、FAQ、交通/住宿、文化条目保持一个完整分段。

单知识库兜底配置：

| 文档族 | 字符分段参考 | Token 分段参考 | overlap |
| --- | --- | ---: | ---: |
| 目的地、景点、文化讲解 | 400-500 字 | 500-700 tokens | 0-50 |
| 美食、FAQ | 350-400 字 | 400-600 tokens | 0-50 |
| 行程模板 | 450-550 字 | 500-700 tokens | 0-50 |
| 交通住宿 | 300-400 字 | 400-600 tokens | 0-50 |

QC 报告和数据字典不进入主检索语料。若平台支持多个子知识库，建议按七类业务库拆分，并按第 3 节路由。

## 6. 更新契约

1. 数据文件、字段结构、ID 规则和星级口径由数据负责人维护；使用方不得直接改写数据定义。
2. 新增或修正条目必须使用既有字段名和顺序，保留唯一 ID、关键词、至少 2 个服务硬信息。
3. 不可靠内容按三级处理：可确证写全；部分可靠只写可确证字段；不可靠整条剔除。
4. 本地修改后运行 `python3 tools/audit_knowledge_base.py`；通过后再上传受影响文件。
5. 景点库按目的地拆分文件，更新一个城市时只需重传对应 `02-景点库-<目的地>.md`；若字段规则或数据字典变化，需同步通知使用方。
6. 禁止用临时提示词掩盖知识库缺口；缺数据应回传数据负责人补库。

## 7. 验收口径

每类库至少验证 2 个正向问法和 1 个别名问法：

| 库 | 示例问法 | 期望命中 |
| --- | --- | --- |
| 目的地 | 北疆 7 天预算 / 成都亲子路线 | 对应目的地卡 |
| 景点 | 紫禁城门票 / 兵马俑怎么预约 | 对应景点卡 |
| 美食 | 潮汕牛肉火锅推荐 / 北京烤鸭去哪吃 | 对应美食卡 |
| 行程 | 北京 4 日亲子线 / 大理丽江 4 日古镇雪山 | 对应行程模板 |
| FAQ | 热门景点怎么抢票 / 下雨天改去哪 | 对应 FAQ 卡 |
| 交通住宿 | 北京机场怎么进市区 / 重庆住哪方便看夜景 | 对应交通或住宿条目 |
| 文化 | 故宫中轴线讲解 / 莫高窟丝路讲解 | 对应文化条目 |

通过标准：

1. 期望条目进入返回结果，且关键字段未被切断。
2. 城市、ID、名称、星级与问法一致。
3. 时效字段保留“待核实”状态，答案能提示官方核实。
4. 无关库不污染主答案。

任一失败时，先判断是分段、路由还是关键词问题；数据缺失则交回数据负责人补库。
